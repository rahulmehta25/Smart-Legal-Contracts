{
  "name": "smart-legal-contracts",
  "private": true,
  "description": "Monorepo root so Vercel can detect Next.js when the project Root Directory is the repository root.",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm run start --prefix frontend"
  },
  "dependencies": {
    "next": "14.2.15"
  }
}
